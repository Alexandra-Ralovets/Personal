# Передача сессии: FlowMeet — 2026-09-21 10:40

## На чём остановились

Постановка для разработки живёт вкладкой «Постановка» в десктопном прототипе (`?view=spec`). В ней: целевая схема (архитектор закрыл вопросы к Пименову, CRM и админу), сверка UC с передачей 26.08, BPMN P1/P2. Пользователь указала эталон оформления требований — Confluence/PRD-231 «Расширение работы с файлами в AU» (глоссарий → цели → пользовательские кейсы проблема/решение → нумерованные ФТ с параметрами и валидацией). Переоформление вкладки под эту форму **начато** (шапка, глоссарий, цели в `sa-draft.js`) и **не доведено**: ниже по файлу ещё старые §§ «клиент и приём / конвейер». Сессию закрыли до конца переписывания. Первым в новой сессии: добить `sa-draft.js` до формы PRD-231, выложить на a1 (scp часто блокируется auto-review).

## Проект в двух фразах

Макеты FlowMeet (desktop/mobile + shared data), не сервер. Живой клиент — передача Пименова 26.08; портал прототипов — a1 `hardware-module`.

## Сделано в этой сессии

- Сняли пилот на отделе продаж из текущего контура.
- Вкладка «Постановка»: система, акторы (не актёры), UC со статусами, ответы архитектора, BPMN P1 (разговор) и P2 (после звонка).
- Договор: реализация по целевой схеме; расхождение с кодом — правят код или явно исключение. Хранилище и пилот — не здесь. Смотреть в FlowMeet, не в Teams.
- Начата перекладка текста под форму PRD-231.
- Прототип: список функционала слева (зелёный/оранжевый), канон miniapp-ui, правило `grid-and-canon.mdc`.

## Не сделано / открыто

1. Дописать `sa-draft.js` в форме PRD-231 (кейсы + ФТ), убрать дубль архитекторских списков.
2. `scp` на `corphub:.../hardware-module/flowmeet-{desktop,shared}/` — BPMN/постановка на a1 могла остаться старой.
3. Отдать Пименову как требование, не как эссе аналитика.
4. Независимый прогон клиента 26.08; физический диктофон (Т7).

## Решения и договорённости

- Пилот продаж — следующий этап, не эта постановка.
- Акторы, не актёры.
- Один stream: звук + hint + policy; `own`/`others` до расшифровки; CRM во время звонка не писать; inbox если нет клиента или низкая уверенность; источник подсказки сейчас = профиль + карточка CRM если опознан.
- Linux плашки — Astra, не «Linux вообще».
- Коммит только правила сетки пользователь отвергла («я говорила про описание») — в этот close входит описание и макеты.

## Куда смотреть в коде

- `flowmeet/flowmeet-shared/sa-draft.js` — постановка + BPMN + незавершённый PRD-каркас
- `flowmeet/flowmeet-desktop/index.html` — вкладка `spec`, стили `.fm-brief` / `.fm-bpmn`
- `flowmeet/flowmeet-shared/data.js` — `PROTO_FEATURES`, подсказки
- `flowmeet/.cursor/rules/grid-and-canon.mdc` — сетка, канон, scp на a1
- Эталон формы: Jira PRD-231 / Confluence page 247405313 (нужен логин)

## Git

- Ветка: main
- Коммит: (заполнится после commit)
- Remote / push: origin
- Не закоммичено: `.env` и секреты (их нет в этом diff). Временный `_patch_sa.py` удалён, не коммитить.

## Проверка

- Локально: десктоп `?view=spec` — глоссарий/цели есть, ФТ в форме PRD могут быть ещё не вместо старых §§.
- Портал: http://a1.ch.corp.infomaximum.com:51863/hardware/flowmeet-desktop/?view=spec — нужна своя сессия a1; scp мог не уйти.

## Риски

- Публикация на corphub часто режется auto-review — не крутить вхолостую, карточка approve.
- Не смешивать BPMN/API-эссе и PRD-форму в одном абзаце для разработки.
- Не обещать 99 % автозаполнения CRM и хранилище в этом контуре.
